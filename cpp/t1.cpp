#include <bits/stdc++.h>
using namespace std;
  
class Node {

    public:
        int data;
        Node* next;// = nullptr;

};

typedef struct node {
    Node* next;
    int data;
    

}node;

int main(){
    
    node a = NULL;
    // Node* head = NULL;

    // Node* curr;
    // for (int i = 0; i < 3; i++)
    // {
    //     if(head ==NULL){
            
    //         head = new Node();
    //         head->data = i;
    //         curr = head;
    //     }else{
    //         curr->next = new Node();
    //         Node *n = curr->next;
    //         n->data = i;
    //         curr = n;
    //     }
    // }

    // cout << head->data;
    // cout << head->next->data;
    // cout << head->next->next->data;

    
}
