#![allow(unused)]

fn main() {
    let words: Vec<String> = vec![String::from("pay"),
                                  String::from("attention"), String::from("practice"), String::from("attend")];
    let pref: String = String::from("at");










    // let mut left = 0;
    // let mut right = words.len() - 1;
    // let mut answer = 0;
    // while left <= right {
    //     if left == right {if words[left].starts_with(&pref) == true { answer += 1;}; break }
    //
    //     if words[left].starts_with(&pref) == true { answer += 1;};
    //     if words[right].starts_with(&pref) == true { answer += 1;};
    //
    //     left += 1;
    //     right -= 1;
    // }
    //
    // println!("{answer}", );
}