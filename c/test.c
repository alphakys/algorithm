#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/*
typedef struct list
{
    int i;
    int index;

} list;




int compare(const list *a, const list *b){

    if((int *)(a->i) > (int *)(b->i)){
        return 1;
    }else if((int *)(a->i) < (int *)(b->i)){
        return -1;
    }else{
        
        if( a->index >  b->index){
            //printf("return %d : \n", 1);
            return 1;
        }else{
            //printf("return %d : \n", -1);
            return -1;
        }
        
    }
    
}
*/

#include <stdio.h>
#include <fcntl.h>

//#include <sstream>
#include <unistd.h>


void main(){

   
}